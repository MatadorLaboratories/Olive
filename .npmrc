# react-konva 19.2.x declares peer react ^19.2, this project pins react@19.0.
# The runtime API surface react-konva uses is identical between those minor
# versions, so we install with legacy-peer-deps. Drop this when we bump
# react in package.json.
legacy-peer-deps=true
